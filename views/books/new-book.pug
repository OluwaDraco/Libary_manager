extends ../layout

block content
    h2 New Book

    form(action="/books/new",method="post")
        include new-form
        p
            input(type="submit", value="Create New Book")
        p 
            a(href="/books") Cancel